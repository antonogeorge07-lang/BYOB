name: Validate

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  swift-build:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Swift version
        run: swift --version

      - name: Build
        run: swift build
